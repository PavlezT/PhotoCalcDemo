name: Call a check for 1 commit in branch

on:
  pull_request:
    branches: [ master ]
    types: [ opened, synchronize, edited, ready_for_review ]

jobs:
  call-one-commit-in-branch:
    runs-on: ubuntu-latest

    steps:
    - name: Git checkout
      uses: actions/checkout@v3
      with:
        fetch-depth: '0'
    - name: Check commits count
      id: check_count
      run: |
        git rev-list --count origin/master..HEAD --first-parent
    - name: Check condution
      if: ${{steps.check_count.outputs.stdout}} > 1
      run: |
        echo "first_parent:$(git rev-list --count origin/master..HEAD --first-parent)"
        echo "normal:$(git rev-list --count origin/master..HEAD)"
        echo "condution:$(${{steps.check_count.outputs.stdout}})"
        exit 1