name: Call a check for 1 commit in branch

on:
  pull_request:
    branches: [ master ]
    types: [ opened, synchronize, edited, ready_for_review ]

jobs:
  call-one-commit-in-branch:
    runs-on: ubuntu-latest

    steps:
    - name: Git checkout
      uses: actions/checkout@v3
      with:
        fetch-depth: '0'
    - name: Check commits count
      run: |
        echo "first_parent:$(git rev-list origin/master HEAD --count --first-parent)"
        echo "normal:$(git rev-list origin/master HEAD --count)"